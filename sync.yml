- hosts: web
  tasks:
  - name: "sync website"
    synchronize:
      src: site3/
      dest: /var/www/{{ domain }}
      archive: no
      checksum: yes
      recursive: yes
      delete: yes
    become: no
